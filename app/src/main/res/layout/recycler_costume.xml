<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="?attr/selectableItemBackgroundBorderless"
    android:baselineAligned="true"
    android:orientation="vertical"
    android:paddingBottom="@dimen/margin_small">

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:orientation="vertical">

        <TextView
            android:id="@+id/type_recycler_costume"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textAlignment="textEnd"
            android:textSize="@dimen/text_size_small" />

        <include
            android:id="@+id/kitten_recycler_costume"
            layout="@layout/include_kitten" />
    </LinearLayout>

    <TextView
        android:id="@+id/title_recycler_costume"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/margin_small"
        android:textAlignment="center" />

    <TextView
        android:id="@+id/contents_recycler_costume"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/margin_recycler_contents"
        android:textAlignment="center"
        android:textColor="@color/color_signature_gray"
        android:textSize="@dimen/text_size_small" />
</LinearLayout>